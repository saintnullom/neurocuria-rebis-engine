# Demo mode is enabled by default and needs no API key.
DEMO_MODE=true

# Set DEMO_MODE=false to enable live analysis. Keep this server-side only.
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4.1-mini
